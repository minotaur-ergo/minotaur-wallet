import React from "react";
import InWalletPage from "../../../layout/InWalletPage";

const DApps = props => {
    return (
        <div style={{textAlign: "center", marginTop: "40%"}}>
            <h2>very soon support for ergo dapps added here</h2>
            <h2>We are working on it</h2>
        </div>
    )
}

export default InWalletPage('dapps')(DApps);
