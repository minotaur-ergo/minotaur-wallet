export type SelectableType = {
  selected?: boolean;
};

export type AssetType = {
  name: string;
  amount: number;
  id: string;
  logoSrc?: string;
};

export type AddressBookType = {
  name: string;
  address: string;
};
