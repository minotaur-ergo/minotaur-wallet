---
'@minotaur-ergo/utils': minor
---

Create `ErgoNodeNetwork` class and write `getHeight`, `getAddressInfo`, `getAddressTransactions` using node API.
Create `network` based on node setting in `findWalletAddresses`.
